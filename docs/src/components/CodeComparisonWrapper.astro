---
import CodeComparison from './CodeComparison';

interface Props {
  rust: string;
  swift: string;
  kotlin: string;
  java?: string;
  csharp?: string;
  typescript?: string;
  python?: string;
  c?: string;
  title?: string;
}

const { rust, swift, kotlin, java, csharp, typescript, python, c, title } = Astro.props;
---

<CodeComparison
  rust={rust}
  swift={swift}
  kotlin={kotlin}
  java={java}
  csharp={csharp}
  typescript={typescript}
  python={python}
  c={c}
  title={title}
  client:load
/>
